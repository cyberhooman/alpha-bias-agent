# Sectors Financial API key from https://sectors.app/api (Insider plan)
# Official auth: Authorization header = raw key (no Bearer prefix)
SECTORS_API_KEY=

# 1 = deterministic mock (default, no network). Set to 0 only when using live API.
MOCK_SECTORS=1

# Optional server bind
HOST=0.0.0.0
PORT=8000
